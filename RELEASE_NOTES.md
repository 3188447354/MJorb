# 1.3.5 默认共享主描述文件

- 普通 IPA 的主应用与全部保留扩展默认共用本轮新生成的主描述文件；不显示模式选择，也不再为每个扩展单独请求 Apple App ID 与描述文件。
- 扩展仍按各自 Bundle ID 重签、嵌入描述文件，并逐项校验设备、证书、有效期和实际权限。**若某扩展请求了主应用没有声明的能力（例如 `increased-memory-limit`），本次签名会自动回退到独立扩展描述文件**（每个扩展各申请一个 App ID），而不是让用户装不上；日志会写明是哪个扩展的哪个能力触发了回退。
- 共享描述文件的 App Group 元数据会同步写入保留扩展，包含文件提供者扩展需要的文档组字段；签名记录仍保留每个实际扩展目标，保证缓存、安装前校验和后续续签正确工作。
- **含扩展的共享模式应用不再尝试“仅更新描述文件”续签**：共享模式只为应用注册一个 App ID，扩展的 App ID 在 Apple 门户里从未存在，续签必然失败（真机报 `SEAL-PROFILE-337`）。这类应用改为走完整重签（仍用共享模式，只需主应用一个名额），无扩展的应用继续走“仅更新描述文件”的快速路径。

【验证】对包含多个扩展的 IPA 签名后，日志应显示“共享主描述文件”“本次需 1 个 App ID”，并对每个主应用/扩展输出一条“描述文件核验：目标=…”；若某扩展请求了主应用未声明的能力，日志应出现“共享主描述文件不适用于本次 IPA”，并改显示“独立扩展描述文件”与“本次需 N 个 App ID”。续签含扩展的应用时应走完整重签流程（而不是“仅更新描述文件”），无扩展的应用仍显示“仅更新描述文件”。

---

# 1.3.4 配对导入与设备验证分离

- 配对助手或手动导入文件后，Seal 会先明确显示“已导入，待验证”。
- 只有 LocalDevVPN 隧道可达时才自动进入真实设备验证；未连接时不会长时间停在“验证中”。
- 用户主动点击 LocalDevVPN 检测时，仍会立即执行完整通道验证。

【验证】关闭 LocalDevVPN 后由配对助手写入文件，Seal 应显示“已导入，待验证”；连接 Wi-Fi 与 LocalDevVPN 后重新打开 Seal 或点击检测，应进入验证并在设备可达时显示“已配对”。

---

# 1.3.3 Seal 自续签结果单抽屉更新

- Seal 结算结果更新时保留同一批量结果抽屉身份，只更新内容；不再先闪现“等待核验”抽屉再弹出“已更新”。

---

# 1.3.2 通知授权状态降噪

- 系统未授予通知权限时，提醒调度会停止在授权检查处，不再反复尝试写入并产生 `SEAL-NOTIFY-002a` 错误。

---

# 1.3.1 Seal 自续签结果即时同步

- 新进程结算 Seal 自替换后，批量结果抽屉会短时主动读取更新后的持久化载荷，立即将“等待新版本核验”切换为“已更新”，不必关闭重开 Seal。

---

# 1.3.0 证书归属与说明完善

- LiveContainer 证书导出仅显示在实际签署该已安装应用的 Apple ID 下，并使用该账号的本机 P12。
- 撤销本机证书后立即更新账号与健康状态，避免短暂显示已失效的旧身份。
- 更新签名、续签与本机凭据说明；帮助页折叠内容改为淡入淡出，避免文字滑入滑出时跳动。
- 开源许可页补充 SideStore 参考组件。

---

# 1.2.9 已安装页刷新诊断降噪

- 已安装页设备查询首次超时仍会记录 `SEAL-INSTALL-707`，保留排查依据；同一次查询的冷却保护与离开页面导致的任务取消不再重复写入警告。
- 刷新仍保持 fail closed：设备状态暂时不可读时，现有列表不会被删除，底层不可取消查询也不会因连续下拉而叠加。
- Seal 自替换后的首次启动改为只有应用页执行“身份对账 → 队列恢复 → 读取结果”的有序链路，不再与根标签页的并行读取竞争旧的“等待新版本核验”载荷。

【验证】设备查询首次超时时日志应有一条 `SEAL-INSTALL-707`；在 20 秒冷却内连续下拉或切换页面，不应再为“查询进行中/冷却中/任务取消”重复产生该警告。续签 Seal 后第一次打开新包，抽屉应直接显示已更新，不应先显示等待核验。

---

# 1.2.8 自替换结算状态同步

- 批量续签 Seal 时，若界面先恢复旧载荷、随后新进程才完成真实身份核验，结果抽屉会识别载荷变更并刷新为最终结论，不再残留“等待 Seal 核验”。
- 已安装页的设备核验失败日志会保留脱敏后的底层原因，能区分应用查询超时与服务返回错误；列表仍保持 fail closed，不会因查询失败删除记录。
- 已安装页下拉刷新使用独立的 2 秒设备查询预算；查询超时后短暂冷却，避免连续下拉累积无法取消的底层查询。签名和安装后的验证预算不变。
- Apple ID 总览将同一账号的 App ID 与证书合并为一次真实远端同步，不再为同一屏幕重复创建两次 Apple 会话；单个 App ID 清单和证书页仍各自按需刷新。
- 已安装页查询若因离开页面被取消，会进入同一冷却路径，不会留下永久“查询中”的刷新锁。
- 设备页可由用户主动导出规范化后的配对文件；导出前会提示其中包含配对私钥，且只交给系统文件保存器，不自动留副本、不写入日志。

【验证】批量续签包含 Seal；新进程日志出现 `SEAL-RENEW-028` 成功后，结果抽屉应显示 Seal 已成功且“等待核验”为 0。进入 Apple ID 总览或下拉刷新时，每个账号应只记录一条“Apple ID 已完整同步”。

---

# 1.2.7 续签结算与通知诊断

- Seal 自替换由新进程确认成功或失败后，批量结果会重新从持久化条目计算，不再残留“等待新版本核验”的旧计数。
- 通知调度失败会记录系统错误域、错误码和脱敏后的具体原因；设置提醒时间失败也会写入日志，便于判断是权限、系统限制还是调度请求问题。
- 已安装页的设备核验改为整轮处理完成后统一刷新，不再因逐项删除重复记录或已卸载应用而用旧快照连续重载列表。
- 下拉刷新“已安装”在设备暂不可达时会保留当前列表并静默结束，只在日志记录诊断，不再反复弹出错误提示。
- 第三方 `profile-only` 续签继续保留 IPA 身份核验和空间预检：它不重新签名、打包或安装，但大 IPA 仍需临时空间核对主应用与扩展身份。

【验证】批量续签包含 Seal 后重新打开应用，日志出现“自替换结算确认”后结果应显示 Seal 成功、等待核验为 0。关闭系统通知权限后，日志应包含 `SEAL-NOTIFY-002a` 及系统错误详情。设备上删除某个已签名应用后刷新“已安装”，该条目应消失且列表不闪回旧内容；设备断开时下拉不应弹窗。

---

# 1.2.6 Seal 自续签结果闭环

- 单项续签、失败项重试与全部续签共用同一续签队列；第三方应用仍按实际条件走描述文件续签或完整重签，Seal 始终排在最后。
- Seal 覆盖安装后不再由旧进程提前显示成功，而是标记为“等待新版本核验”；新进程读取运行中 Seal.app 的真实签名身份后，才将批量结果和续签队列结算为成功或失败。
- 启动时先完成 Seal 自替换身份核验，再恢复被中断续签队列，避免已验证的 Seal 被误记为“结果未知”。

【验证】批量续签包含 Seal 时，旧进程退出前日志应出现“等待新进程核验”；重新打开 Seal 后仅在日志出现“自替换结算确认”时显示 Seal 已更新。候选未落盘时应显示失败或需要核验，绝不显示成功。

---

# 1.2.5 日志时间统一北京时间

- 日志正文中的描述文件创建/到期时间、证书生效/到期时间与 Seal 自替换结算时间统一使用北京时间，并明确写出 `+08:00` 偏移。
- 日志首列继续使用北京时间 `yyyy-MM-dd HH:mm:ss`；不再出现首列是北京时间、同一行的业务时间却是 UTC `Z` 的混用。

【验证】签名或续签后检查日志：业务时间应形如 `2026-09-30T20:34:56+08:00`，不应再以 `Z` 结尾。

---

# 1.2.4 Seal 自续签结算可审计

- Seal 自替换后会记录新运行包是否已与候选身份一致，包括实际描述文件到期日；旧包未替换和需要恢复会明确记录为非成功。
- 批量续签恢复时可从导出日志区分“批量结果恢复”与“Seal 自身已由新运行包结算”。

【验证】续签 Seal 后重新打开应用；日志应出现“自替换结算确认”，并显示新描述文件到期日。若未替换成功，则应出现“仍在安装前身份”或“需要恢复”，不得显示为成功。

---

# 1.2.3 续签轨道与实际步骤同步

- 符合条件的描述文件续签会把连接设备、验证账号、核对应用与证书、核对 App ID、申请描述文件依次对应到五格轨道。
- 描述文件阶段按实际已取得的份数推进最后一格；这条路径不会再保留实际不会发生的重新签名、传输和安装格。
- 需要完整更新的应用以及 Seal 自身仍保持原有的完整重签和安装轨道。

【验证】更新后续签一个日志确认“仅更新描述文件”的已安装第三方应用；五格轨道应在“已取得 N / N 份描述文件”时走到最后一格，日志不应出现签名、传输或安装阶段。

---

# 1.2.2 续签状态更准确

- 已安装的第三方应用符合条件时，Seal 会只更新设备端描述文件；界面会明确显示“描述文件续签完成”，不会再误说重新签名或安装了 IPA。
- 需要完整更新时仍会显示“续签并安装成功”，续签开始、进行中和完成后的文字都会按本轮实际路径变化。
- 批量续签日志会记录实际 Bundle ID 和逐份确认的描述文件，方便核对结果。
- 若 Apple 门户中已找不到当前应用原有的 App ID，快速续签会停止并引导完整重签，不会在免费账号下悄悄创建新的 App ID。

【验证】更新后续签一个已安装的第三方应用；若日志显示“仅更新描述文件”，过程不会经过签名、传输或安装阶段。Seal 自身仍按完整更新路径处理。

---

# 1.2.1 修复 iOS 17.0–17.3.1 本机配对

- 配对助手在 iOS 17.0–17.3.1 自动生成本机配对（Lockdown）文件；系统版本尚未读取完成时不再误生成远程配对文件。
- Seal 导入时校验 Lockdown 配对文件是否包含完整的设备、主机和证书身份，避免导入无效占位文件后才在连接时失败。
- 本机配对安装和续签改为 AFC 暂存加安装服务链路，不再错误调用只适用于远程配对的 RSD 安装入口。
- 安装重试前会真正重建与设备的 RSD 连接。原先那处复位逻辑的判据写在连接状态被清除之后，因此恒不成立、从未生效，导致重试一直复用已经断开的连接。

【验证】iOS 17.0–17.3.1：用新版 Windows 配对助手生成并写入配对文件；Seal 设置页应显示“本机配对”，随后连接 LocalDevVPN 后可签名安装和续签。

---

# 1.2.0 优化部分应用无法签名

修复部分应用签不上的问题（体积大、扩展多的更容易遇到）。

【更新方式一】电脑签名安装

① 到内测群下载最新的 v1.2.0 安装包

② 用电脑签名后安装

【更新方式二】应用内更新

① 挂外区节点，点「更新」按钮下载

② 出现「Seal 续签」抽屉后，先检查版本是否正确

③ 确认无误，连接 Wi-Fi 和 LocalDevVPN，开始续签

【验证】能正常签名并安装，就是成功。

【失败】把「文件」App → 我的 iPhone → Seal → Seal-log.txt 私发群主。

---

# 1.1.16：完成 Seal 自替换安装，停止自动重装

- 修复 Seal 自续签安装刚开始就主动挂起自身的问题；挂起会同时冻结当前进程持有的 `installation_proxy` 连接，导致 installd 永远无法返回完成结果，新描述文件也没有写入 Seal.app。
- 自替换期间保持 Seal 运行，直到 installd 完成覆盖；覆盖成功后由 iOS 自然终止旧进程，重新打开时再核对实际落盘的描述文件、证书和到期日。
- 保留 1.1.15 的持久化单次恢复限制，即使设备端仍拒绝安装，同一签名成品也不会形成启动后无限自动重装。
- 应用详情页在完整证书序列号前明确显示“Apple ID 证书”，便于核对账号与实际签名身份。

验证：`Seal-log(6).txt` 中每轮都有“自更新安装：第1次”，但没有任何“安装返回”或“Seal.app 落盘核验”，与挂起点严格对应；结构守护禁止安装链路再次引用主动挂起逻辑。Swift 编译、单测与 UI 回归由本次 GitHub Actions 执行，真机安装 1.1.16 后核验新描述文件日期和仅一次自替换。

---

# 1.1.15：阻止 Seal 启动恢复无限重装

- 同一份已签 Seal IPA 最多自动恢复安装一次，恢复资格在安装前持久化；进程被替换或重启后不会重置计数。
- 自动恢复内部再次准备同一 profile 时保留“已经尝试”状态，彻底切断启动→重装→重启→再次重装循环。
- 一次恢复后仍不匹配时停止后台安装并记录 `SEAL-INSTALL-736`；用户手动续签生成新 profile 后才获得新的一次恢复机会。

验证：新增跨重启、重复 prepare、产生新 profile 三段回归测试；完整 Swift/UI/Rust/IPA 验证由本次 GitHub Actions 执行。

---

# 1.1.14：修正 Seal 自续签假成功与日志目录消失

- Seal 自续签恢复 SideStore 上游实际使用的 installation_proxy Install 覆盖操作，保留同 Bundle ID 的应用数据容器。
- 安装成功必须直接读取设备上 Seal.app 内嵌描述文件，并逐项匹配 Bundle ID、Team、profile UUID 与证书；系统 profile 库只作为诊断证据。
- 启动发现仍是旧描述文件时，后台复用上轮已签且通过完整性校验的 IPA 自动恢复安装，不重新请求 Apple 或生成另一份描述文件。
- 启动即恢复 `Documents/Seal-log.txt` 镜像，Files 中持续显示 Seal 目录；日志新增实际 Seal.app 身份与系统 profile 库的分项结果。

验证：本地 Rust 编译检查、结构守护和发布安全检查；Swift 编译、单测与 UI 回归由本次 GitHub Actions 执行。真机需安装 1.1.14 后确认 Seal 自续签到期日更新且 Files 中日志目录保留。

---

# 1.1.12：证书轮换与完整 7 天续签闭环

- 免费团队没有可覆盖新一轮 7 天签名的本机证书时，按真实 Apple Portal 链路撤销不可用证书并立即创建、保存新的本机签名身份；付费团队只在 Apple 明确返回 3022 名额上限后进入同一恢复链。
- 每撤销一张证书都立刻同步 Keychain 与账号绑定，避免中途退出后继续误用已撤销证书；新证书自身也必须通过完整周期校验。
- 证书轮换后自动重签同账号下受影响的已安装应用；Seal 始终最后安装，自续签从证书处理开始持有后台执行任务。
- 每个主应用和扩展的描述文件都核对申请时间、UUID、Bundle ID、设备 UDID、团队、证书序列号及剩余有效期；Apple 返回旧文件时在同一会话立即重新申请。
- 日志新增证书复用判定、轮换顺序、撤销持久化、新证书日期、描述文件创建与到期日期、安装衔接及受影响应用恢复结果，导出仍走统一脱敏链路。

验证：本地结构守护、发布安全变异检查与差异检查通过；云 CI 状态见本次提交对应的 Actions。真机链路由后续安装 1.1.12 后核验导出日志中的证书与描述文件日期。

## 1.1.11：外部签名后的首次续签与证书诊断

- 爱思等工具安装的 Seal 首次续签时，识别缺少本机私钥的签名身份，保留当前证书并尝试建立本机身份。
- 区分证书请求无效与真实名额上限，提供恢复说明，减少无效重复检查。
- 证书清理保护 Seal、其他已装应用和设备描述文件正在引用的证书；无法核验时保留。
- 保留重新登录及绑定恢复过程中的历史签名材料。
- 自更新前保存核验目标，重启后确认运行包与私钥；不再在安装返回后直接撤旧证。
- 修正批量续签提前报完成、旧包启动显示新有效期的问题。

验证：代码 `6fd6416` 的 Actions `34929845356` 已通过 Release 编译/打包、rork-sign 测试、Swift 单测与 UI 回归步骤；产物核对为 1.1.11 / 50。真机首次接管和第二次复用仍待验证。若 Apple 确实不允许新增证书且外部私钥不可取得，仍需原电脑工具辅助恢复。

## 既有安全整改

- 安装失败不再自动卸载已有应用；不再仅凭旧应用存在宣告续签成功。
- 证书名额不足时不再自动撤销其他证书，改为人工确认处理。
- 加强主可执行文件校验；嵌套外层压缩包提示先解压后导入真正 IPA。
- 收紧批量重试，修正 Seal Team 选择顺序及取消等待行为。
- 快速构建默认只出包、不发布；发布标签须与 IPA 版本一致。

本次推送用于编译与回归验证，未自动发布 Release。
