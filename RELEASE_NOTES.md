# 1.3.20 已安装页不会再「把装着的应用当成没装」而删掉本地记录

- 修复**已安装页可能误删本地记录与文件**（真实缺口）。这个页面会向设备查询「某个 App 还在不在」，不在就删掉本地记录并清掉本地文件。问题在于：**这条查询在底层只返回「是 / 否」两种答案**，而设备通道半坏时它会把「问不到」也答成「否」（而且不报错）—— 于是一次通道抖动就可能把一批其实还装着的应用连记录带文件一起删掉。
  - **现在**：动手删之前，先拿**确定装着的 Seal 自己**问一次做**阳性对照**。对照问不到、或答成「没装」⇒ **本轮一条记录都不删**，只在日志里留一条 `SEAL-INSTALL-739`（**警告级**）说明「通道此刻不可信」。
  - 这不是新发明的做法：同一个查询的其它使用处（设备描述文件清理、设备端扫回、安装后的核验）**一直**都带这道网，只有已安装页这条删除路径漏了。
- 修改**Seal 自身续签的「后台保活」日志文案**。它原来写「后台保活已启动，覆盖证书、描述文件、签名和安装」，而这句话写在**续签路径判定之前** —— 走「仅更新描述文件」时根本没有重签、没有安装，于是同一秒里会出现「覆盖…签名和安装」与「不会重新签名、打包或安装 IPA」两行**自相矛盾**的话。
  - **现在**：只陈述保活的目的 ——「避免续签过程中被锁屏或切后台挂起」。
- 修复**「Seal 自身记录同步失败」这条日志说不出原因**。它原来只有一句话，不带底层错误。
  - **现在**：带上底层错误原因。这条日志在「刚装完 Seal、还没导入配对文件」的启动窗口里**必然**出现两次，属于正常现象 —— 有了原因就能一眼看出是这种情况，而不是真的出问题。

【验证】① 打开「已安装」页下拉刷新：**不应**因为通道抖动而丢失记录；若通道确实不可信，日志里应出现 `SEAL-INSTALL-739`（**警告级**）且列表**保持原样**。② 续签 Seal 自己（走「仅更新描述文件」那条路）时，日志里**不应**再出现「覆盖证书、描述文件、签名和安装」；应只有「…避免续签过程中被锁屏或切后台挂起」。③ 刚装完 Seal、还没导入配对文件时打开：`SEAL-SELF-REG-001` 应带出底层原因（不再是一句无信息的话）。

---

# 1.3.19 界面说人话：进度文案与实际动作对齐，「装完又重签一次」不再出现

- 修复**第一次把 Seal 装到手机上、打开后签不了东西**。重装 Seal 会清空钥匙串，于是「本机没有这张证书的私钥」—— 以前这时会直接报「签名证书数量已达上限」之类的拦截，看不出真正的原因。
  - **现在**：在「证书序列号」那一行直接写明「**需要重新签名一次获取本机证书**」（详情页 / 操作抽屉给完整说明，进度卡片给紧凑版），并且**本次自动改为完整重签**。重签会拿到一张本机有私钥的证书并写回记录，**之后再续签就回到「只更新描述文件」的快路径**，不会每次重装。
  - 判据：日志里出现 `SEAL-PROFILE-364`（级别为**警告**，不是失败）—— 它意味着「本轮为了拿到本机证书多签了一次，之后不会再有」。
- 修复**「签名到安装步骤后又重签一次」**。这不是多签了一次，而是**证书轮换的恢复流程**在装完之后重签那些用旧证书签过的 App（旧证书已被撤销，不重签它们会立刻打不开）。问题出在**抽屉**：这条恢复流程的阶段被原样写进了你正在看的那个会话，于是刚显示「正在验证安装」，紧接着又跳回「正在连接设备 / 正在准备应用文件 / 正在准备证书」。
  - **现在**：**只有属于当前这个 App 的阶段**才更新抽屉；恢复流程只在自己内部记账，不再抢走进度显示。Seal 自替换的「回主屏」照旧按「**Seal** 被覆盖安装」判定，与当前会话是谁无关。
- 修改**进度卡片上的「已等待 m:ss」**。它原来每个阶段都在计时，读起来像「这一步卡了多久」，而它其实只对**设备安装阶段**有意义（installd 在安装期间不回报任何进度，计时是唯一能证明进程还活着的信号）。
  - **现在**：只有**设备安装阶段**保留「设备正在安装，此阶段没有进度回报 · 已等待 m:ss」，其余阶段不再显示计时。
- 修改**签名 / 续签阶段文案，与真实动作对齐**（不做的事不写，位置不乱）：
  - 「正在申请证书」→「**正在准备证书**」。这一阶段实际做的是「读远端证书列表 → 查本机有没有对应私钥 → 决定复用还是撤销重建」，**绝大多数时候是复用** —— 写成「申请」等于宣称一件没发生的事。
  - 「正在注册 Bundle ID」→「**正在核对 App ID**」。它先读账号里已有的列表，只对缺失的才发注册请求（真机日志里常见「需新注册 0 个」）。
  - 「已注册 N / N 个 Bundle ID」→「**已准备 N / N 个 App ID**」。这个数字是「**已就绪**的个数」（复用的和新建的都算），不是「本次新注册了几个」。

【验证】① 重装 Seal 后第一次签名一个 App ⇒ 应**成功**（而不是被拦截）；「证书序列号」那一行应出现「需要重新签名一次获取本机证书」；日志里应出现 `SEAL-PROFILE-364`（警告级）。之后再续签同一个 App ⇒ 应走「仅更新描述文件」，**不应**再出现 `SEAL-PROFILE-364`。② 签名 / 续签过程中，抽屉**不应**在装完之后跳回「正在连接设备 / 正在准备应用文件 / 正在准备证书」；若本轮发生了证书轮换，日志里会出现「证书轮换事务：自动续签 N 个受旧证书影响的已安装应用」——这是**正常**的恢复流程，只是不再显示在你的进度里。③ 进度卡片**不应**再显示「已等待 m:ss」；进入设备安装阶段后应出现「设备正在安装，此阶段没有进度回报 · 已等待 m:ss」。④ 日志与界面上**不应**再出现「正在申请证书」「正在注册 Bundle ID」「已注册 N / N 个 Bundle ID」。

---

# 1.3.18 恢复证书自动轮换：1.3.17 的「绝不撤销运行中 Seal 的证书」把免费账号锁死了

- 修复**免费账号「下载 Seal 啥也干不了，全被拦截了」**（1.3.17 引入的回归）。1.3.17 为了修「撤销运行中 Seal 的证书 → 自更新失败 → Seal 打不开」，改成**绝不自动撤销** Seal 正在用的那张证书。但免费账号**只有一个**证书槽位，而 Seal 自己的证书在覆盖安装后**必然**丢失本机私钥 ⇒ 它往往就是**唯一**可轮换的证书 ⇒ 被排除之后就再也建不出新证书 ⇒ 签任何 App、续签任何 App 都报「签名证书数量已达上限（3022）」。
  - **现在**：**恢复自动轮换**。Seal 的证书**可以**被撤销，但**排在最后**（先撤普通证书，别无选择才动它）；撤销前会在日志里说清后果，撤销后由证书轮换的恢复流程在**本轮末尾**用新证书重签并重装 Seal。
  - **为什么这样是安全的**：真正让 Seal 变砖的是**自更新安装失败**，不是撤销本身 —— 撤销之后的恢复流程本来就在（1.3.16 已修好它的判据）；而「不撤」的代价是整条签名 / 续签链路**完全不可用**。
  - 顺带把「单槽位接管」的验收规格（`CertificateTakeoverPolicy`）也改成同一个口径：运行中 Seal 的签名者证书**排最后但仍进候选**（原来写的是「永远不进候选」，那条规格连同它的单测正是这次死锁被固化成契约的地方）。

【验证】① 免费账号（Seal 自己的证书没有本机私钥）点「签名并安装」一个第三方 App ⇒ 应**成功**；日志里应出现「证书轮换：撤销 …，原因=无本机私钥，运行中Seal=是」与「证书轮换：本轮将撤销 1 张 Seal 正在使用的证书（…）；Seal 将在本事务末尾以新证书重新安装」；**不应**再出现「证书轮换：候选只剩 Seal 正在使用的证书」和 `SEAL-CERT-204b`。② 续签 Seal 自己 ⇒ **不应**再出现 `SEAL-CERT-204b`。③ 撤销之后应看到 Seal 被重签并重装（日志里「开始自替换安装」→ 回主屏）。

---

# 1.3.17 续签终于「真的」只换描述文件：Seal 自己也不再重装、不再被中断

- 修复**续签仍然要完整重装**。1.3.15 已经改掉「按应用身份一刀切」（Seal 自己不再被排除在快路径外），但真机上**一次都没生效**：Seal 自身的记录由另一个模块维护，而那份记录里**从来不写**「已安装产物」「设备标识」「签名目标」这几个字段，快路径的准入判据偏偏要求它们齐全 ⇒ Seal 每次续签仍然走完整重签 + 覆盖安装自己。
  - **现在**：除了「记录是否完整」，多了一条**以运行中的产物为准**的准入 —— Seal 能直接读出自己运行包里的真实签名身份（描述文件 UUID、签名证书序列号、Team），身份读得出来且完整就放行。这与上游 SideStore 的做法一致：它的续签准入看的正是**已安装产物的签名证书**，而不是数据库记录。
  - **不是放宽**：运行包身份读不出来（主程序或扩展的签名信息损坏）时，照旧回落完整重签；身份里的 Bundle ID 与记录对不上也不放行。
- 修复**「设备端核验没确认」就把整轮续签降级成完整重装**。续签前会核验「设备上是否真有记录里的那份描述文件」，核验没明确通过（通道抖动、枚举失败、记录与设备有偏差）就自动回落成完整重签 —— 真机日志里每次续签都因此重装了一遍。
  - **现在**：这项核验**只写日志、不再决定走哪条路**。上游 SideStore 的续签管线**根本不枚举设备描述文件**：它从 Apple 门户取一份**新**描述文件直接注入设备，准入只核验「已安装产物的签名证书是否还有效」。真正的安全网在**注入之后** —— 每一份注入的描述文件都会从设备读回来逐份确认，注入没落地照样报错，绝不会谎报成功。
- 修复**续签中途把 Seal 自己弄坏**。当 Apple 账号下的证书丢失了本机私钥、而其中一张正是**当前运行的 Seal** 在用的证书时，旧实现会**自动撤销它**来腾出名额 —— 撤销之后 Seal 只能靠「重新安装自己」恢复，那一步失败 Seal 会立刻打不开（历史上确实这样变砖过）。真机日志里这条链路完整重演了一次：撤销 Seal 在用的证书 → 重签 Seal → 强制退出旧进程 → 自更新中止 → 续签被打断。
  - **现在**：**绝不自动撤销「运行中 Seal」正在用的证书**。还有别的可轮换证书时照常轮换（Seal 那张本来也排在最后）；只剩它一张时不再动手，交回原有的「名额满」处理路径（那条路径同样会跳过 Seal 的证书，不会静默弄坏它）。
- 修复**「无法刷新已安装应用」在续签过程中误报**。已安装页的设备核验走的是同步底层调用，和签名 / 安装 / 续签**共用同一条设备会话** —— 续签正在进行时它必然超时，然后把一个失败弹窗推给用户。真机日志里三次误报全部落在续签与自更新进行中。
  - **现在**：有前台操作（签名 / 安装 / 续签 / 证书管理）正在进行时，已安装页的设备核验**整体跳过** —— 不探测、不报错、不弹窗，列表保留本地快照，等空闲了再核验。
  - 注意这里**没有**去动安装链路的验证预算：根因是并发抢会话，不是预算太短；调大预算只会让每次失败等更久。

【验证】① 在「已安装」页点「续签全部」（列表里含 Seal 自己）⇒ 整轮应**全部**走「仅更新描述文件」，日志里每项都出现「续签路径已确认：仅更新描述文件」与「profile-only 续签已由设备端逐份读回确认」，**不再出现**「需要完整重签并安装」「开始自替换安装」「SEAL-SELF-109」。② 续签进行中下拉刷新「已安装」页 ⇒ **不应**再弹「无法刷新已安装应用」，日志应出现「已安装页设备核验跳过：有前台操作正在进行」。③ 续签完成后日志里**不应**出现「证书轮换：撤销 …，运行中Seal=是」。

---

# 1.3.16 修好「不管什么错都怪 VPN」：只有真正跟本地隧道有关的失败才把你送去 VPN 页

- 修复**「只要失败就跳 VPN 设置页」**。以前任何 `SEAL-INSTALL-*` 错误（安装族）都会把你送到「LocalDevVPN」设置页 —— 包括**跟本地隧道毫无关系**的那些：设备存储空间不足、免费账号 3 应用上限、砸壳包的 DRM 元数据残留、安装超时、签名包损坏需重新签名、需要重启 Seal…… 你点弹窗上唯一的「恢复」按钮之后，被送到一个**解决不了你问题**的页面，只能自己再退回来。
  - **原因**：跳转判据写成了一条**前缀判断** —— `错误码以 "SEAL-INSTALL-" 开头 ⇒ 去 VPN 页`。而安装族一共几十个错误码，绝大多数与本地隧道无关。
  - **现在**：跳转只按**显式码集合**判定，集合里只收「**提示文案本身就在让你去检查 LocalDevVPN**」的那几条（本地隧道未就绪 / 设备未响应 / 本地通道连接超时 / 无法经本地隧道连到设备 等）。**其余失败不再跳转** —— 弹窗自己说明下一步（该删应用、该重新签名、该重启 Seal、该换账号），不再把你扔到 VPN 页。
  - 加了一道**同源闸门**：凡是提示文案写着「检查是否打开 LocalDevVPN」的失败，它的错误码就必须在这个集合里 —— 防止以后新增失败时「文案让用户去查 VPN，却没有任何按钮能送他过去」。
- 修复**配对类失败被给出「重新安装」按钮**。`SEAL-INSTALL-703`（设备配对不可用）与 `SEAL-INSTALL-707`（无法刷新已安装应用）用的是 `SEAL-INSTALL-` 前缀，以前会落进「通道类、可以安全重跑安装」的兜底 ⇒ 界面给出「重新安装」按钮。而**重装修不好一份失效的配对文件**。
  - **现在**：这两条被显式归入**配对族**，按钮变成「重新配对设备」；它们的码集合只有**一处**定义（`InstallFailureActionPolicy.pairingCodes`），界面、路由、单测共用同一份，不会再各抄一份而漂移。
- 修正**过时的提示文案**。`SEAL-INSTALL-701` 的说明还在写「**付费账号的 Seal 会自动拉起内置隧道**」—— 而内置隧道（`SealTunnel`）**早已移除**，Seal 一律依赖**外部 LocalDevVPN**。同时把 5 处「免费账号需先安装并打开外部 LocalDevVPN 软件」改成「Seal 依赖外部 LocalDevVPN 软件提供本地隧道」：**隧道依赖与账号类型无关**，不要再按免费/付费区分。

【验证】① 触发一次**与隧道无关**的失败（例如装一个超过设备剩余空间的包，或让免费账号装第 4 个应用）⇒ 弹窗点「恢复」/「知道了」后**不应**跳到 LocalDevVPN 设置页。② 触发一次**真正**的通道失败（关掉 LocalDevVPN 再续签）⇒ 弹窗应能跳到 LocalDevVPN 设置页。③ 在「设置 → 设备配对」里把配对文件换成无效的，再点续签 ⇒ 若出现配对类错误，按钮应是「重新配对设备」而**不是**「重新安装」。

---

# 1.3.15 Seal 自己的续签也改成「只更新描述文件」，续签时不再重启 Seal

- **续签 Seal 自己时不再重新安装、不再重启 Seal**。以前点「续签全部」跑到 Seal 这一项时，Seal 必须**重新签名并覆盖安装自己** ⇒ 进程被系统替换掉 ⇒ 批量续签会在**最后一项中断**，重开后提示「上次续签被中断，1 个应用的结果未知，需要重新核验」，只能手动再点一次。
  - **原因**：续签本来有一条「只换描述文件、不重装」的快路径，但准入判据里有一条**按应用身份**的一刀切 —— 「Seal 自己一律走完整重签」。所以 Seal 是唯一**用不上**这条快路径的应用，而它恰好是每次批量续签都要跑的那一个。
  - **现在**：Seal 与其它已安装应用走**同一条**判据 —— 记录完整就只换描述文件（向设备注入新的描述文件并逐份读回校验），整个续签过程**不重启 Seal**，批量续签不再中断，也不会再出现「结果未知」。批量续签的结果统计也同步改成按**本次实际走的路径**记账，而不是按「是不是 Seal」猜。
  - **安全边界没有放松**：记录不完整（缺已安装的签名产物 / 缺签名身份 / 缺目标记录）时，Seal 照旧走完整重签 + 重新安装 —— 改掉的只是「按身份一刀切」，不是判据本身。每次 Seal **版本升级**后记录会被重建，那一次仍会走完整重签，这是预期行为。
  - 与上游 SideStore 的做法一致：它的续签管线（`fetchProvisioningProfiles` → `cacheResignedMetadata` → `refreshApp`）**从不重签、从不重装**，对它自己也只注入描述文件。

【验证】在「已安装」页点「续签全部」（列表里含 Seal 自己）⇒ 跑到 Seal 那一项时 **Seal 不应重启**（界面不闪退、不回到启动页），整轮一次跑完、不再提示「结果未知」。日志里应出现「续签路径已确认：仅更新描述文件」与「profile-only 续签已由设备端逐份读回确认」，且**不再出现**「开始自替换安装」。

---

# 1.3.14 修复「重装 Seal 找回的应用，导入 IPA 覆盖更新后点续签仍然失败」

- 修复**重装 Seal 之后自动找回的应用（见 1.3.13），导入该应用的 IPA 覆盖更新、记录也已进入已安装列表，点「立即续签」却怎么点都失败**的问题。真机现象：日志报 `SEAL-AUTH-110`「缺少签名账号记录：这个应用没有记录上次签名使用的 Apple ID，无法自动续签」，而**紧挨着的前一行**日志已经写明「开始续签：<应用名>，Apple ID：…」—— 说明账号**已经解析出来了**，是后面的校验又把它拦下。
- **原因**：扫回重建的记录**没有「上次用哪个 Apple ID 签名」这一项** —— 扫回发生在「配对成功之后、添加 Apple ID 之前」，那一刻账号库还是空的；导入覆盖更新时又**如实沿用**了这个空值。而续签的账号校验里有一条写死的前置条件「记录必须有 Apple ID，否则直接拒绝」⇒ 这类记录**永远**续签不了。
  - 真正决定签名身份的是**开发者团队（Team）**，不是「记录里记的 Apple ID」：同一个 Team 的账号签出来的是**同一个签名身份**，覆盖安装到设备上的也还是同一个应用。所以「记录里没记 Apple ID」只说明本地没记过，**不影响**能不能安全续签。
- **现在**：
  - 记录里**没有** Apple ID 时不再直接拒绝，而是**按团队（Team）继续校验**：团队一致 ⇒ 正常续签；团队不一致（换过 Team）⇒ **仍然拒绝**并提示；团队信息也读不到 ⇒ **仍然拒绝**。**不是**放开校验，只是把「本地没记过」与「用了别的账号」分开对待。
  - 续签成功后会把本次使用的 Apple ID **写回记录**，这条记录就此**自愈**，以后走正常路径。
  - 两种「回退」在日志里**分开记**，便于事后判断成因：`SEAL-AUTH-111a`（记录里的 Apple ID 已被删除、重新添加后 ID 变了）与 `SEAL-AUTH-111b`（记录里从来没记过 Apple ID —— 即设备端扫回重建的记录）。

【验证】① 只卸载 Seal（保留其他自签应用）→ 重装 Seal → 在「已安装」页看到自动找回的应用 → **导入该应用的 IPA 并选择「覆盖更新」** → 在已安装列表里点「立即续签」⇒ 应**正常进入签名并安装**，不再报「缺少签名账号记录」。② 续签完成后再看日志，应出现 `SEAL-AUTH-111b`（说明本次是按 Team 回退续签的）。③ 再点一次「立即续签」⇒ 这次应走正常路径（记录里已经有 Apple ID 了），日志**不再**出现 `SEAL-AUTH-111b`。

---

# 1.3.13 重装 Seal 后能找回之前装的应用；所有有效期精确到秒

- 新增**「重装 Seal 之后自动找回之前装过的应用」**。以前只卸载 Seal、其他用 Seal 签名的应用不卸载时，重装后它们**进不了 Seal 的已安装列表**（记录文件随 Seal 一起没了，设备上的应用却还在），既看不到有效期、也没法在列表里管理。
  - **做法**：Seal 启动时的维护作业会读一遍**设备端的描述文件**，把「Bundle ID 形如 `<原始包名>.seal.<你的 Team>`、但本地没有记录」的那些应用**补回已安装列表**。
  - **补回的记录是「诚实」的**：它只有设备上真实存在的身份（Bundle ID、Team、描述文件、到期时间），**没有本地原始 IPA** —— 所以**点续签时的确认面板**会明确写「此记录由设备端扫回重建：本地没有它的原始 IPA，无法重签或续签；如需续签请重新导入该应用的 IPA」，确认后也会给出同一句提示，而不是抛一个看不懂的「找不到文件」。（这段提示存在记录的导入警告里，本仓只在签名确认面板展示，**应用详情页没有**。）
  - **不会凭空多出记录**：候选必须同时满足「是 Seal 生成过的形态」「Team 属于你已添加的账号」「设备端确实装着」；其中「设备端确实装着」先拿 Seal 自己**做阳性对照**，对照不通过（通道在说假话）就**整轮一条都不建**。
  - **删掉就不会自己回来**：从已安装列表移除某条记录后，Seal 会记住这次删除，扫回**不会**再把它加回来（此前只删记录不卸载应用，下次启动就会被重新捞出来）。
  - **不会抢 Seal 自己**：Seal 的记录仍由它自己的自注册逻辑管理，扫回不参与；扩展也不会被当成独立应用建记录。
- 所有**有效期 / 创建时间**改为**精确到秒**（`2026-09-25 14:17:03`）。免费账号只有 7 天有效期，此前只显示到**分钟**，同一分钟内续签两次显示完全一样 —— 而「描述文件的创建时间应当是刚刚」正是续签后唯一要核验的事，分钟粒度看不出区别。
- 修复**点「取消」被记成「安装超时」失败**：安装等待期间取消，底层那次安装其实还在跑，但界面会把它算成一次「安装超时」（日志 `SEAL-INSTALL-702t`），并在签名历史里留下一条**失败**记录。现在取消会被如实识别为取消（不弹窗、不记失败、不重试）。
- 修复**同一条超时日志里两个数字互相矛盾**：心跳写「已等待 46 秒」、紧接着的结论行却写「已等待 803 秒」（后者其实是等待上限）。现在结论行报**实际**等待，上限另作括注。

【验证】① 只卸载 Seal（保留其他自签应用）→ 重新安装 Seal → 打开「已安装」页，之前那些应用应**自动出现**（名称是它的原始包名，有效期与设备端描述文件一致）；对其中一条点「立即续签」，确认面板里应写明「无本地原始 IPA」。② 在已安装列表里删掉其中一个 → 重启 Seal，它**不应**再回来。③ 随便打开一个应用详情页看有效期，应显示到**秒**。④ 续签过程中点「取消」→ 不应弹出「安装超时」，签名历史里不应出现一条失败。

---

# 1.3.12 导入新版可以直接「覆盖更新」已安装的应用

- 修复**导入一个已安装应用的新版本 IPA 时，它只出现在「待签名」页、点签名又报「Bundle ID 冲突」**的问题 —— **Seal 自己导入新版也一样装不上**。
- **原因**：导入时**根本没有去找已安装的同名应用** —— 「要替换的记录」在代码里被写死成「无」，于是每次导入都新建一条待签记录；这条新记录又从旧记录那里继承了**同一个签名后的 Bundle ID**，签名时两条记录争同一个身份，被「Bundle ID 冲突」拦下。Seal 自己还多一层：只有当本地记录里的身份信息与导入包**对得上**时才走自更新，对不上就退化成普通记录 —— 于是「连 Seal 自己也装不了」。
- **现在**：导入一个与**已安装**应用同原始 Bundle ID 的 IPA 时，确认页会显示「**更新方式：覆盖更新「应用名」（v旧 → v新）**」，主按钮变成「**覆盖更新**」：
  - 走**覆盖安装**：复用那条已安装记录的签名身份（账号 / 证书 / 描述文件 / 签名后 Bundle ID 全部沿用），手机上通过系统覆盖安装**替换**原应用，**不会多出第二个图标**，也不占额外的免费账号设备名额；
  - 结果落在**「已安装」**列表（版本号就地更新），随后直接点「签名 / 安装」即可；
  - 不想覆盖时可以点**「新建副本（不覆盖）」**：按原行为新建一条待签名记录，用另一个 Bundle ID 签出并存 —— 这条路径**保持可用**；
  - 覆盖更新会替换记录与本地 IPA 文件，属于**破坏性操作**，所以必须由你在确认页**明确选择**，不会静默替换；
  - 覆盖成功与「目标已失效、已回落为新建」都会写进日志（`SEAL-IPA-212` / `SEAL-IPA-213`），便于事后排查。
- **顺带修掉两个会让覆盖更新静默失效的隐患**：① 覆盖后写文件用的目录名与记录标识不一致，会让签名阶段去一个**不存在的目录**取包（覆盖**必然**失败）；② 覆盖时没有清掉旧版本的签名产物，可能把**旧版本**重新装回设备（看起来像「更新没生效」）。
- **Seal 自己导入新版**也加了兜底：即使本地记录里的身份信息与导入包对不上（历史遗留），只要导入的就是**当前正在运行**的 Seal，就仍按「自更新」处理，不再新建一条普通记录。

【验证】① 先在手机上装好一个自签应用，再导入该应用**更高版本**的 IPA ⇒ 确认页应显示「覆盖更新「…」（v1.0 → v2.0）」；点「覆盖更新」后，该应用应出现在**「已安装」**列表且版本号已更新，手机上点开是新版本，**不会多出第二个图标**。② 同一个 IPA 再导入一次并点「新建副本（不覆盖）」⇒ 应新增一条**待签名**记录，原应用不受影响。③ 导入 Seal 自己的新版 ⇒ 应按自更新处理（与之前一致），不再出现「Bundle ID 冲突」。

---

# 1.3.11 修复「单个应用续签永远失败，点续签全部却能成功」

- 修复**删除 Apple ID 再重新添加之后，除 Seal 自己以外的应用点「立即续签」怎么点都失败，而点「续签全部」却能全部成功**的问题。真机现象：点单个应用续签，日志报「无法在设备端确认当前应用的描述文件身份」（`SEAL-PROFILE-362`）；同一时段点「续签全部」三个应用全过。
- **原因**：续签有一条「只换描述文件、不重新签名」的快速路径。走这条路径前要先问设备「记录里那份描述文件还在不在」。**问设备的结果本来有三种**——确认在、确认不在、**问不到**（通道正忙/刚被别处占用/解析失败）——但旧代码把它压成了「是/否」两种，只要不是「确认在」就**直接判定失败并结束本轮**。于是通道偶尔抖动一次，这个应用就**永久**续签不了：单点和「续签全部」调的是同一段代码、同一份记录、同一张证书，差别只在通道时序 —— 单点恰好撞上通道忙，批量恰好躲开。
  - 真机日志佐证：三次单点失败（`10:56:34` / `10:57:00` / `10:58:04`）都落在「已安装页设备探测刚超时」（`SEAL-INSTALL-707`）之后的十几秒内，而同一时段「续签全部」三次全过（`10:57:14` / `10:57:24` / `10:57:45`）。
- **现在**：
  - 「问不到」**不再等于「设备上没有」**，也**不再等于失败**：改为**自动回落完整重签**（重新向 Apple 申请描述文件并重新签名安装，完全不读本地旧记录，安全性不受影响），日志记 `SEAL-PROFILE-363` 并写明是哪一种「问不到」。
  - 这个「问设备」的步骤**加了 30 秒上限**：它是不能中途取消的底层调用，此前一旦通道忙，整条续签会一直被拖住（真机实测从「续签路径已确认」到失败隔了 16 秒）。
  - 回落后的完整重签**不再被免费账号的「3 个应用」安装预检误拦**（续签是覆盖安装、不新增设备槽位）。此前只有快速路径豁免，回落路径会撞上另一条报错，等于把一种失败换成另一种。
  - 「续签路径」提示改为**核验之后**才显示：不会再出现「先说只换描述文件、随后报错」的误导。
- 另外**补上删除 Apple ID 时的告知**：确认弹窗现在会写明「会一并清除本机保存的该账号签名凭据 —— 之后重新添加同一 Apple ID 时，续签需要更换签名证书，这 N 个应用会被自动重新签名安装一次」。这正是「点一个应用续签、却看到三个应用都在签名」的原因（本机证书私钥随账号一起被删，重新添加后只能撤销旧证书另建新的，而撤销会让旧证书签名的应用全部失效，必须重签它们和 Seal）。**该行为本身是必要的**——不重签那些应用会打不开——但此前完全没有提示。

【验证】① 删除 Apple ID 并重新添加，然后点**单个**业务应用「立即续签」：应成功；日志里若出现 `SEAL-PROFILE-363`（而不是 `SEAL-PROFILE-362`），说明设备核验这次没问通、已自动改走完整重签，**这是正常的、也会成功**。② 点「续签全部」仍应全部成功。③ 删除 Apple ID 的确认弹窗应写明会清除本机签名凭据、并自动重签关联应用。

---

# 1.3.10 修复「续签其他应用时 Seal 自己变砖」

- 修复**签名或续签其他应用之后，Seal 自己打不开（点开即闪退）、「VPN 与设备管理」里的描述文件也不见了**的问题。真机现象：Seal 图标还在，但点开就闪退；到「VPN 与设备管理」里已经看不到它的描述文件。
- **原因有三层，缺一不可**：
  1. **证书轮换判断错了对象**：免费 Apple ID 只有一个证书槽位，本机没有旧证书私钥时只能「先撤销旧的、再建新的」。但判断「哪一张是 Seal 正在用的」那段代码**只在签名 Seal 自己时才去读**；签名别的应用时它恒判「不是 Seal 在用」—— 于是 Seal 的命根子证书被当成普通证书撤销了，日志里那句「当前 Seal 在用证书排在最后」也就成了空话。
  2. **把「还在装」当成了「装失败」**：安装返回只代表**上传完成**，真正的替换由系统在后台异步完成。这中间重启会读到旧包，此前会**直接判定失败并永久关闭这次替换的记录**，而记录里的描述文件已经在签名阶段被乐观改写成新的 —— 从此记录和设备现实对不上。
  3. **按错位的记录清理描述文件**：维护作业拿上面那份错位记录当「必须保留的那一份」，于是把设备上**真正在用**的那份描述文件删掉了。
- **现在**：
  - 撤销证书前**始终**读出 Seal 真实使用的证书并把它排到最后；真要撤销时，日志会明确写出「本轮将撤销 Seal 正在使用的证书 …」以及后果。
  - 上传完成后的 90 秒内视为「替换窗口」：窗口内重启**不再**判定失败，而是保留记录、下次启动再判（日志 `SEAL-SELF-114`）。
  - 一旦确认候选确实没装上，**立即按运行中的 Seal 回补记录**（描述文件 / 证书序列号 / 安装状态），维护作业不会再据此删错描述文件；回补失败会记 `SEAL-SELF-115`。
  - 清理描述文件时，Seal 自己**只信运行时读到的真实描述文件**；读不到就整条不参与「留哪一份」的判断 —— 宁可少清一份，绝不删错。

【验证】① 用一个本机没有私钥的 Apple ID 签名一个第三方应用，日志里应出现「本轮将撤销 … Seal 正在使用的证书」而不是「运行中Seal=否」；Seal 应在本轮末尾自动重新安装并正常打开。② 若在替换窗口内重启，日志应出现 `SEAL-SELF-114`（而不是 `SEAL-SELF-111`）；窗口过后仍未装上才出现 `SEAL-SELF-111`，且此后不应再把 Seal 自己正在用的那份描述文件删掉。

---

# 1.3.9 删除 Apple ID 后第三方应用仍无法续签

- 修复**删掉 Apple ID 再重新添加后，除 Seal 自己以外的应用仍无法续签**的问题：点「立即续签」会提示「Apple ID 不匹配」并让你去「重新验证 Apple ID」，但那个账号其实已经验证过了、也没有别的账号可切。
- **原因**：1.3.7 修好了「**选哪个账号**」（按同 Team 找到重新添加的账号），但签名前**还有一道独立的校验**，仍然拿记录里的旧账号标识去和当前账号做**精确比对** —— 重新添加 Apple ID 会生成新的账号标识，两者必然对不上，于是在这道校验里被拦下，连后面的「同 Team」判断都走不到。Seal 自己走的是另一条分支（只比 Team、不比标识），所以只有它能续签。
- **现在**：这道校验也改成按「记录里的账号是否**真的还在**」分流 —— 账号还在（确实换了账号）时仍然拒绝；账号已被删除（记录过期）时交给同 Team 判断：**同 Team 放行、换 Team 仍拒绝**。删过账号的第三方应用终于能续签了。
- **日志会说清用了哪个账号**：记录账号已失效而按同 Team 续签时，会记一条 `SEAL-AUTH-111a`（含应用名、Team、实际使用的账号），不再只有一句「续签成功」。
- **已安装应用列表不会再「删一半」**：设备查询在刷新中途失败时（例如刚启动、通道还没就绪），此前可能已经删掉了前几条「设备上查不到」的记录才中止。现在改成**先把所有记录问完、再统一删除**，任何一条查询失败都整轮中止、**一条都不删**，并在日志里写明「本轮未删除任何记录」。
- **安装与重试不再做一次无效的连接重置**：此前安装前、重试前、设备查询前都会重置一次连接提供者，但那个动作只清 Swift 侧对象、清不掉底层的会话缓存 —— 既解决不了「死连接」，还可能把正在服务的安装连接拆掉。已移除（三条路径全部清零）。

【验证】用同一个 Apple ID 签好 2–3 个应用后，在「我的」里删除该 Apple ID，再重新添加同一个 Apple ID：对其中**任意一个业务应用**（不要选 Seal 自己）点「立即续签」，应能正常完成，日志里出现 `SEAL-AUTH-111a`、不应再出现 `SEAL-AUTH-111`；批量续签应显示全部成功（`failed: 0, needsAction: 0`）。若改用**另一个 Team** 的 Apple ID，点续签应仍明确提示「开发者团队不匹配」，而不是静默换账号。

---

# 1.3.8 最低支持调整为 iOS 17.4

- **Seal 现在要求 iOS 17.4 及以上**：iOS 16.0–17.3.1 的设备将**无法安装**本版本。
- **原因**：iOS 17.4 是 Apple 引入远程配对所依赖的 `CoreDeviceProxy` 的那一版 —— 这是**唯一**一条既不需要「本机配对」（Lockdown）、也不需要开启 LocalDevVPN 的安装链路。低于 17.4 的设备要么根本装不上 Seal，要么只能走必须开 VPN 的本机配对，稳定性无法保证。
- **仍停留在 iOS 16.0–17.3.1 的用户请继续使用 1.3.7 或更早版本**：电脑端配对助手仍会为这些设备生成「本机配对」文件，该功能**没有移除**，只是不再随 Seal 的新版本一起提供。
- 本版本同时包含 1.3.7 的「删除 Apple ID 后其他应用无法续签」修复。

【验证】在「我的 → 关于」里「最低支持」应显示 iOS 17.4；在 iOS 17.4 及以上设备上安装本版本应正常；iOS 17.3.1 及以下设备应无法安装。

---

# 1.3.7 删除 Apple ID 后其他应用无法续签

- 修复**删掉 Apple ID 再重新添加后，除 Seal 自己以外的应用都无法续签**的问题：此前点「立即续签」会直接提示去添加 Apple ID，即使列表里明明有一个已验证的账号。
- **原因**：应用记录里保存的账号标识在账号被删除后就失效了 —— 重新添加同一个 Apple ID 会**新建一条账号记录（新的标识）**，而这些应用指向的仍是**已经被删除的那条旧记录**；Seal 自己每次启动都会从自身签名信息里回补账号，所以只有它不受影响。
- **现在**：续签前会先核对记录里的账号是否**真的还存在**。若不存在，Seal 会按**同一个 Team**（即同一个签名身份）去找可用账号 —— 只有同 Team 才能保证应用的 Keychain 访问组与 App Group 不变；**找不到同 Team 的账号时不再悄悄换一个账号**，而是明确说明「原来由哪个 Team 签名、该账号已被删除」，并保留用户手动指定的账号优先。
- **界面不再与实际行为相反**：应用列表此前会显示「未记录·自动选择」，而续签其实会被拒绝。现在账号失效时用告警色显示「记录账号已失效」/「记录账号需重新验证」，与续签时的判断完全一致。
- **续签被拒现在会写日志**：此前这种情况一条日志都没有，排障时无从下手。现在会记录应用名、Bundle ID、记录的账号与 Team、账号总数，并区分「记录账号需要重新验证」（`SEAL-AUTH-104f`）与「记录的账号已被删除且没有同 Team 账号」（`SEAL-AUTH-104g`）—— 两者的下一步动作不同。

【验证】用同一个 Apple ID 签好 2–3 个应用后，在「我的」里删除该 Apple ID，再重新添加同一个 Apple ID：此时对其中**任意一个业务应用**（不要选 Seal 自己）点「立即续签」，应能正常完成，日志里不再出现「续签被拒」；列表里该应用不应显示「未记录·自动选择」这类与实际行为相反的文案。若改用**另一个 Team** 的 Apple ID 重新添加，点续签应明确提示「找不到原来的签名账号」并说明原 Team，而不是静默换账号继续。

---

# 1.3.6 证书轮换后的自动恢复与失败可见性

- 修复**证书轮换后受影响的已安装应用不再被自动重签**的问题：当本机缺少证书私钥、签名时触发证书轮换（撤销旧证书并签发新证书）时，Seal 会重新签名安装所有仍在使用被撤销证书的已安装应用。此前这条自动恢复的判据里多了一个「账号必须完全一致」的条件，而「本机无私钥」恰恰要求该账号被删除过、重新添加后又会生成一个新的账号记录 —— 两个条件互相排斥，导致恢复**从未真正执行过**；被撤销证书的应用会一直打不开，而日志里只有一句「自动续签 0 个」。
- **撤销已经发生、后续步骤却失败时不再静默**：证书一旦被撤销，用它的应用会立即无法打开。此前若这之后的签名或安装失败，Seal 只提示「安装失败」，不会说明还有哪些应用已经失效，日志里也没有线索。现在会在失败提示与日志里写明「本轮已撤销 N 张旧证书，M 个受影响应用未能自动恢复」，并保留原有的重试引导。
- **批量续签的逐项失败现在会写进日志**（此前只更新界面）：日志里能看到是哪个应用、什么错误码、失败原因，不必再靠推断。
- **描述文件清理的 dump 失败会写明尝试了几次**：此前失败时这个数字总是 1，分不出「一次都没重试」与「重试 3 次仍失败」，而两者的下一步动作完全不同。
- **安装后验证失败现在区分两种情况**：如果连 Seal 自己都查不到（说明设备通道不可信），提示为「无法确认安装结果」并引导检查 LocalDevVPN，而不是报「安装后验证失败」让用户去重装一个其实已经装好的应用；确实没装上时仍提示重新安装。

【验证】在一台删除过 Apple ID、重新添加后签名触发证书轮换的设备上，日志应出现「证书轮换事务：自动续签 N 个受旧证书影响的已安装应用」（N ≥ 1），随后每个受影响应用各有一条「受影响应用 … 已重新签名安装」。若在撤销之后的步骤失败，弹窗与日志都应出现「本轮已撤销 N 张旧证书，M 个受影响应用未能自动恢复」。批量续签有失败项时，日志应逐项出现「批量续签：第 i/N 项未成功 —— …」。设备不可达导致描述文件清理中断时，日志应出现「dump 尝试 3 次」。设备通道不可信时安装后验证应报「无法确认安装结果」而非「安装后验证失败」。

---

# 1.3.5 默认共享主描述文件

- 普通 IPA 的主应用与全部保留扩展默认共用本轮新生成的主描述文件；不显示模式选择，也不再为每个扩展单独请求 Apple App ID 与描述文件。
- 扩展仍按各自 Bundle ID 重签、嵌入描述文件，并逐项校验设备、证书、有效期和实际权限。**若某扩展请求了主应用没有声明的能力（例如 `increased-memory-limit`），本次签名会自动回退到独立扩展描述文件**（每个扩展各申请一个 App ID），而不是让用户装不上；日志会写明是哪个扩展的哪个能力触发了回退。
- 若 **Apple 判定主应用声明的某组能力参数无效**（3001）并按空能力重发，本轮会**把嵌入同一份主描述文件的全部目标（主应用与各扩展）的请求集一并清空**，而不是只清主应用自己 —— 否则逐项校验会误报“权限缺失”，把一个本来可以正常安装运行的应用拦下。日志会写明被拒的是哪个 App ID、本次提交了哪些能力、以及清空了几个目标。
- 共享描述文件的 App Group 元数据会同步写入保留扩展，包含文件提供者扩展需要的文档组字段；签名记录仍保留每个实际扩展目标，保证缓存、安装前校验和后续续签正确工作。
- **共享模式的应用同样支持“仅更新描述文件”续签**：共享模式下设备端本来就只登记主应用这一份描述文件，续签只需取回并注入这一份（不重新签名、不打包、不上传整包）。续签沿用**应用当初实际签名时用的策略**（记录字段），历史记录没有该字段时按独立描述文件处理。
- 修复**签名完成后抽屉一直停在“正在验证安装”**的问题：签名过程中若触发了证书轮换（本轮重新签发了证书），Seal 会在同一条流程里接着重签受影响的已装应用、**包括 Seal 自己**；此前该子流程的阶段不会回传，“该回主屏”的判断也错用了本次签名的那个应用，导致 Seal 的自替换等不到旧进程退出，抽屉最长会停在“正在验证安装”直到超时。现在阶段回传会带上“这是哪个应用的阶段”，Seal 自己需要回主屏时会立即执行，抽屉也会显示真实阶段。

【验证】对包含多个扩展的 IPA 签名后，日志应显示“共享主描述文件”“本次需 1 个 App ID”，并对每个主应用/扩展输出一条“描述文件核验：目标=…”；若某扩展请求了主应用未声明的能力，日志应出现“共享主描述文件不适用于本次 IPA”，并改显示“独立扩展描述文件”与“本次需 N 个 App ID”。若 Apple 判主应用的能力参数无效（3001），日志应出现“能力被 Apple 拒（3001）⇒ 本轮该描述文件不授予任何能力；已一并清空 N 个共享它的目标的请求集”（N = 该应用的全部目标数，共享模式下即主应用 + 扩展数），并**不再**出现“权限缺失”。续签含扩展的应用时日志应显示“仅更新描述文件；不会重新签名、打包或安装 IPA”，并在结束时给出“注入 N 份描述文件、覆盖 M 个目标”（共享模式下 N=1、M=扩展数+1）。若签名过程中触发了证书轮换（受影响应用含 Seal 自己），日志应先出现“阶段进入：正在验证安装（Seal）”，随后紧跟“上传完成，1.2 秒后判断前台状态并回主屏”与“触发回主屏转场”，抽屉不应停在“正在验证安装”。

---

# 1.3.4 配对导入与设备验证分离

- 配对助手或手动导入文件后，Seal 会先明确显示“已导入，待验证”。
- 只有 LocalDevVPN 隧道可达时才自动进入真实设备验证；未连接时不会长时间停在“验证中”。
- 用户主动点击 LocalDevVPN 检测时，仍会立即执行完整通道验证。

【验证】关闭 LocalDevVPN 后由配对助手写入文件，Seal 应显示“已导入，待验证”；连接 Wi-Fi 与 LocalDevVPN 后重新打开 Seal 或点击检测，应进入验证并在设备可达时显示“已配对”。

---

# 1.3.3 Seal 自续签结果单抽屉更新

- Seal 结算结果更新时保留同一批量结果抽屉身份，只更新内容；不再先闪现“等待核验”抽屉再弹出“已更新”。

---

# 1.3.2 通知授权状态降噪

- 系统未授予通知权限时，提醒调度会停止在授权检查处，不再反复尝试写入并产生 `SEAL-NOTIFY-002a` 错误。

---

# 1.3.1 Seal 自续签结果即时同步

- 新进程结算 Seal 自替换后，批量结果抽屉会短时主动读取更新后的持久化载荷，立即将“等待新版本核验”切换为“已更新”，不必关闭重开 Seal。

---

# 1.3.0 证书归属与说明完善

- LiveContainer 证书导出仅显示在实际签署该已安装应用的 Apple ID 下，并使用该账号的本机 P12。
- 撤销本机证书后立即更新账号与健康状态，避免短暂显示已失效的旧身份。
- 更新签名、续签与本机凭据说明；帮助页折叠内容改为淡入淡出，避免文字滑入滑出时跳动。
- 开源许可页补充 SideStore 参考组件。

---

# 1.2.9 已安装页刷新诊断降噪

- 已安装页设备查询首次超时仍会记录 `SEAL-INSTALL-707`，保留排查依据；同一次查询的冷却保护与离开页面导致的任务取消不再重复写入警告。
- 刷新仍保持 fail closed：设备状态暂时不可读时，现有列表不会被删除，底层不可取消查询也不会因连续下拉而叠加。
- Seal 自替换后的首次启动改为只有应用页执行“身份对账 → 队列恢复 → 读取结果”的有序链路，不再与根标签页的并行读取竞争旧的“等待新版本核验”载荷。

【验证】设备查询首次超时时日志应有一条 `SEAL-INSTALL-707`；在 20 秒冷却内连续下拉或切换页面，不应再为“查询进行中/冷却中/任务取消”重复产生该警告。续签 Seal 后第一次打开新包，抽屉应直接显示已更新，不应先显示等待核验。

---

# 1.2.8 自替换结算状态同步

- 批量续签 Seal 时，若界面先恢复旧载荷、随后新进程才完成真实身份核验，结果抽屉会识别载荷变更并刷新为最终结论，不再残留“等待 Seal 核验”。
- 已安装页的设备核验失败日志会保留脱敏后的底层原因，能区分应用查询超时与服务返回错误；列表仍保持 fail closed，不会因查询失败删除记录。
- 已安装页下拉刷新使用独立的 2 秒设备查询预算；查询超时后短暂冷却，避免连续下拉累积无法取消的底层查询。签名和安装后的验证预算不变。
- Apple ID 总览将同一账号的 App ID 与证书合并为一次真实远端同步，不再为同一屏幕重复创建两次 Apple 会话；单个 App ID 清单和证书页仍各自按需刷新。
- 已安装页查询若因离开页面被取消，会进入同一冷却路径，不会留下永久“查询中”的刷新锁。
- 设备页可由用户主动导出规范化后的配对文件；导出前会提示其中包含配对私钥，且只交给系统文件保存器，不自动留副本、不写入日志。

【验证】批量续签包含 Seal；新进程日志出现 `SEAL-RENEW-028` 成功后，结果抽屉应显示 Seal 已成功且“等待核验”为 0。进入 Apple ID 总览或下拉刷新时，每个账号应只记录一条“Apple ID 已完整同步”。

---

# 1.2.7 续签结算与通知诊断

- Seal 自替换由新进程确认成功或失败后，批量结果会重新从持久化条目计算，不再残留“等待新版本核验”的旧计数。
- 通知调度失败会记录系统错误域、错误码和脱敏后的具体原因；设置提醒时间失败也会写入日志，便于判断是权限、系统限制还是调度请求问题。
- 已安装页的设备核验改为整轮处理完成后统一刷新，不再因逐项删除重复记录或已卸载应用而用旧快照连续重载列表。
- 下拉刷新“已安装”在设备暂不可达时会保留当前列表并静默结束，只在日志记录诊断，不再反复弹出错误提示。
- 第三方 `profile-only` 续签继续保留 IPA 身份核验和空间预检：它不重新签名、打包或安装，但大 IPA 仍需临时空间核对主应用与扩展身份。

【验证】批量续签包含 Seal 后重新打开应用，日志出现“自替换结算确认”后结果应显示 Seal 成功、等待核验为 0。关闭系统通知权限后，日志应包含 `SEAL-NOTIFY-002a` 及系统错误详情。设备上删除某个已签名应用后刷新“已安装”，该条目应消失且列表不闪回旧内容；设备断开时下拉不应弹窗。

---

# 1.2.6 Seal 自续签结果闭环

- 单项续签、失败项重试与全部续签共用同一续签队列；第三方应用仍按实际条件走描述文件续签或完整重签，Seal 始终排在最后。
- Seal 覆盖安装后不再由旧进程提前显示成功，而是标记为“等待新版本核验”；新进程读取运行中 Seal.app 的真实签名身份后，才将批量结果和续签队列结算为成功或失败。
- 启动时先完成 Seal 自替换身份核验，再恢复被中断续签队列，避免已验证的 Seal 被误记为“结果未知”。

【验证】批量续签包含 Seal 时，旧进程退出前日志应出现“等待新进程核验”；重新打开 Seal 后仅在日志出现“自替换结算确认”时显示 Seal 已更新。候选未落盘时应显示失败或需要核验，绝不显示成功。

---

# 1.2.5 日志时间统一北京时间

- 日志正文中的描述文件创建/到期时间、证书生效/到期时间与 Seal 自替换结算时间统一使用北京时间，并明确写出 `+08:00` 偏移。
- 日志首列继续使用北京时间 `yyyy-MM-dd HH:mm:ss`；不再出现首列是北京时间、同一行的业务时间却是 UTC `Z` 的混用。

【验证】签名或续签后检查日志：业务时间应形如 `2026-09-30T20:34:56+08:00`，不应再以 `Z` 结尾。

---

# 1.2.4 Seal 自续签结算可审计

- Seal 自替换后会记录新运行包是否已与候选身份一致，包括实际描述文件到期日；旧包未替换和需要恢复会明确记录为非成功。
- 批量续签恢复时可从导出日志区分“批量结果恢复”与“Seal 自身已由新运行包结算”。

【验证】续签 Seal 后重新打开应用；日志应出现“自替换结算确认”，并显示新描述文件到期日。若未替换成功，则应出现“仍在安装前身份”或“需要恢复”，不得显示为成功。

---

# 1.2.3 续签轨道与实际步骤同步

- 符合条件的描述文件续签会把连接设备、验证账号、核对应用与证书、核对 App ID、申请描述文件依次对应到五格轨道。
- 描述文件阶段按实际已取得的份数推进最后一格；这条路径不会再保留实际不会发生的重新签名、传输和安装格。
- 需要完整更新的应用以及 Seal 自身仍保持原有的完整重签和安装轨道。

【验证】更新后续签一个日志确认“仅更新描述文件”的已安装第三方应用；五格轨道应在“已取得 N / N 份描述文件”时走到最后一格，日志不应出现签名、传输或安装阶段。

---

# 1.2.2 续签状态更准确

- 已安装的第三方应用符合条件时，Seal 会只更新设备端描述文件；界面会明确显示“描述文件续签完成”，不会再误说重新签名或安装了 IPA。
- 需要完整更新时仍会显示“续签并安装成功”，续签开始、进行中和完成后的文字都会按本轮实际路径变化。
- 批量续签日志会记录实际 Bundle ID 和逐份确认的描述文件，方便核对结果。
- 若 Apple 门户中已找不到当前应用原有的 App ID，快速续签会停止并引导完整重签，不会在免费账号下悄悄创建新的 App ID。

【验证】更新后续签一个已安装的第三方应用；若日志显示“仅更新描述文件”，过程不会经过签名、传输或安装阶段。Seal 自身仍按完整更新路径处理。

---

# 1.2.1 修复 iOS 17.0–17.3.1 本机配对

- 配对助手在 iOS 17.0–17.3.1 自动生成本机配对（Lockdown）文件；系统版本尚未读取完成时不再误生成远程配对文件。
- Seal 导入时校验 Lockdown 配对文件是否包含完整的设备、主机和证书身份，避免导入无效占位文件后才在连接时失败。
- 本机配对安装和续签改为 AFC 暂存加安装服务链路，不再错误调用只适用于远程配对的 RSD 安装入口。
- 安装重试前会真正重建与设备的 RSD 连接。原先那处复位逻辑的判据写在连接状态被清除之后，因此恒不成立、从未生效，导致重试一直复用已经断开的连接。

【验证】iOS 17.0–17.3.1：用新版 Windows 配对助手生成并写入配对文件；Seal 设置页应显示“本机配对”，随后连接 LocalDevVPN 后可签名安装和续签。

---

# 1.2.0 优化部分应用无法签名

修复部分应用签不上的问题（体积大、扩展多的更容易遇到）。

【更新方式一】电脑签名安装

① 到内测群下载最新的 v1.2.0 安装包

② 用电脑签名后安装

【更新方式二】应用内更新

① 挂外区节点，点「更新」按钮下载

② 出现「Seal 续签」抽屉后，先检查版本是否正确

③ 确认无误，连接 Wi-Fi 和 LocalDevVPN，开始续签

【验证】能正常签名并安装，就是成功。

【失败】把「文件」App → 我的 iPhone → Seal → Seal-log.txt 私发群主。

---

# 1.1.16：完成 Seal 自替换安装，停止自动重装

- 修复 Seal 自续签安装刚开始就主动挂起自身的问题；挂起会同时冻结当前进程持有的 `installation_proxy` 连接，导致 installd 永远无法返回完成结果，新描述文件也没有写入 Seal.app。
- 自替换期间保持 Seal 运行，直到 installd 完成覆盖；覆盖成功后由 iOS 自然终止旧进程，重新打开时再核对实际落盘的描述文件、证书和到期日。
- 保留 1.1.15 的持久化单次恢复限制，即使设备端仍拒绝安装，同一签名成品也不会形成启动后无限自动重装。
- 应用详情页在完整证书序列号前明确显示“Apple ID 证书”，便于核对账号与实际签名身份。

验证：`Seal-log(6).txt` 中每轮都有“自更新安装：第1次”，但没有任何“安装返回”或“Seal.app 落盘核验”，与挂起点严格对应；结构守护禁止安装链路再次引用主动挂起逻辑。Swift 编译、单测与 UI 回归由本次 GitHub Actions 执行，真机安装 1.1.16 后核验新描述文件日期和仅一次自替换。

---

# 1.1.15：阻止 Seal 启动恢复无限重装

- 同一份已签 Seal IPA 最多自动恢复安装一次，恢复资格在安装前持久化；进程被替换或重启后不会重置计数。
- 自动恢复内部再次准备同一 profile 时保留“已经尝试”状态，彻底切断启动→重装→重启→再次重装循环。
- 一次恢复后仍不匹配时停止后台安装并记录 `SEAL-INSTALL-736`；用户手动续签生成新 profile 后才获得新的一次恢复机会。

验证：新增跨重启、重复 prepare、产生新 profile 三段回归测试；完整 Swift/UI/Rust/IPA 验证由本次 GitHub Actions 执行。

---

# 1.1.14：修正 Seal 自续签假成功与日志目录消失

- Seal 自续签恢复 SideStore 上游实际使用的 installation_proxy Install 覆盖操作，保留同 Bundle ID 的应用数据容器。
- 安装成功必须直接读取设备上 Seal.app 内嵌描述文件，并逐项匹配 Bundle ID、Team、profile UUID 与证书；系统 profile 库只作为诊断证据。
- 启动发现仍是旧描述文件时，后台复用上轮已签且通过完整性校验的 IPA 自动恢复安装，不重新请求 Apple 或生成另一份描述文件。
- 启动即恢复 `Documents/Seal-log.txt` 镜像，Files 中持续显示 Seal 目录；日志新增实际 Seal.app 身份与系统 profile 库的分项结果。

验证：本地 Rust 编译检查、结构守护和发布安全检查；Swift 编译、单测与 UI 回归由本次 GitHub Actions 执行。真机需安装 1.1.14 后确认 Seal 自续签到期日更新且 Files 中日志目录保留。

---

# 1.1.12：证书轮换与完整 7 天续签闭环

- 免费团队没有可覆盖新一轮 7 天签名的本机证书时，按真实 Apple Portal 链路撤销不可用证书并立即创建、保存新的本机签名身份；付费团队只在 Apple 明确返回 3022 名额上限后进入同一恢复链。
- 每撤销一张证书都立刻同步 Keychain 与账号绑定，避免中途退出后继续误用已撤销证书；新证书自身也必须通过完整周期校验。
- 证书轮换后自动重签同账号下受影响的已安装应用；Seal 始终最后安装，自续签从证书处理开始持有后台执行任务。
- 每个主应用和扩展的描述文件都核对申请时间、UUID、Bundle ID、设备 UDID、团队、证书序列号及剩余有效期；Apple 返回旧文件时在同一会话立即重新申请。
- 日志新增证书复用判定、轮换顺序、撤销持久化、新证书日期、描述文件创建与到期日期、安装衔接及受影响应用恢复结果，导出仍走统一脱敏链路。

验证：本地结构守护、发布安全变异检查与差异检查通过；云 CI 状态见本次提交对应的 Actions。真机链路由后续安装 1.1.12 后核验导出日志中的证书与描述文件日期。

## 1.1.11：外部签名后的首次续签与证书诊断

- 爱思等工具安装的 Seal 首次续签时，识别缺少本机私钥的签名身份，保留当前证书并尝试建立本机身份。
- 区分证书请求无效与真实名额上限，提供恢复说明，减少无效重复检查。
- 证书清理保护 Seal、其他已装应用和设备描述文件正在引用的证书；无法核验时保留。
- 保留重新登录及绑定恢复过程中的历史签名材料。
- 自更新前保存核验目标，重启后确认运行包与私钥；不再在安装返回后直接撤旧证。
- 修正批量续签提前报完成、旧包启动显示新有效期的问题。

验证：代码 `6fd6416` 的 Actions `34929845356` 已通过 Release 编译/打包、rork-sign 测试、Swift 单测与 UI 回归步骤；产物核对为 1.1.11 / 50。真机首次接管和第二次复用仍待验证。若 Apple 确实不允许新增证书且外部私钥不可取得，仍需原电脑工具辅助恢复。

## 既有安全整改

- 安装失败不再自动卸载已有应用；不再仅凭旧应用存在宣告续签成功。
- 证书名额不足时不再自动撤销其他证书，改为人工确认处理。
- 加强主可执行文件校验；嵌套外层压缩包提示先解压后导入真正 IPA。
- 收紧批量重试，修正 Seal Team 选择顺序及取消等待行为。
- 快速构建默认只出包、不发布；发布标签须与 IPA 版本一致。

本次推送用于编译与回归验证，未自动发布 Release。
