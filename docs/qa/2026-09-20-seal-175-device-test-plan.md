# Seal-175 真机测试清单（换签名器之后的第一轮）

> ## ✅ **执行结果（2026-09-20 09:36，日志 `Seal-log(28).txt`）**
>
> **定版 `构建 1.1.16 (175)`** ✓ —— 三个问题全部有答案：
>
> | # | 问题 | 结果 |
> |---|---|---|
> | 1 | 装完能不能启动（FairPlay） | ✅ **LiveContainer ＋ 抖音都能打开** ⇒ 风险排除 ✓ |
> | 2 | 大包会不会被 CPU 预算杀掉 | ✅ **没被杀** —— 抖音全程走完（重签 78 秒 / 打包 43 秒 / 安装 121.6 秒）✓ |
> | 3 | 日志里能不能定位崩溃点 | ⚠️ 仍是已知退化（本次没崩所以没暴露）✓ |
>
> **⇒ 换签名器全部验收通过** ✓✓ 详细时间线与两处「日志不可信」见
> [`2026-09-19-signer-swap-to-sidesign.md`](./2026-09-19-signer-swap-to-sidesign.md) 第五节 ✓
>
> ⚠️ 抖音**第一次**失败是 `SEAL-APPID-304`（免费账号 10 个 App ID 名额满 ✗）——
> **不是缺陷** ✓，第一次其实已把前几个 App ID 建好，**重试即成功** ✓。

- 产物：`Seal-175/Seal_1.1.16.ipa`（**未签名**，22.6 MB，`com.mjorb.seal`，最低 iOS 17.0）
- 版本 / 构建：**1.1.16 (175)** —— 日志表头应显示 `构建 1.1.16 (175)`
- 换的东西：签名器 `rork-sign` → 上游 `SideSign` + `CodeSignKit`
- CI 状态：**run#175 全绿**（`build-package` / `swift-regression` / `signer-tests` 都过）

> ⚠️ **重装 Seal 会清空 keychain** ⇒ 旧证书私钥全丢 ⇒ 每个 team 需要**重新创建一张证书**（这是正常的，
> 但**如果撤销成功而创建失败**，那个账号会变成「0 张证书」，用它签过的 App 全部打不开 ✗ ——
> 出现这种情况立刻停下来找我）。

---

## 第一步：Seal 自己能起来（约 30 秒）

用**现在手机上那个 Seal**（或你平时的方式）签 `Seal_1.1.16.ipa` 并安装，然后：

- [ ] 打开 Seal，首页能正常加载
- [ ] 账号列表还在、状态正常

**这一步只证明「包本身没坏」** —— ⚠️ **不验证 FairPlay**（Seal 是被**旧签名器**签的）。

---

## 第二步：签一个「小包」，装完**立刻点开它** ← 🔴 这一步才是 FairPlay 的验收点

挑一个**体积小、没有扩展**的 App（随便一个测试 App 即可），走完整流程：

- [ ] 签名成功
- [ ] 安装成功
- [ ] 🔴 **装完立刻点开那个 App**

| 结果 | 结论 |
|---|---|
| ✅ 能正常启动、能用 | **FairPlay 那条风险排除** ✓ —— 上游不清 `cryptid` 也没影响，可以放心继续 |
| ❌ 图标装上了但**一点开就退回桌面** | **就是 FairPlay 那条** ✗ —— 立刻把崩溃日志给我，我把原型补丁（历史提交 `b548021`）移植到 `CodeSignKit` |
| ❌ 装都装不上 / 签名就失败 | 另一类问题，把日志给我（大概率是证书 / 描述文件 / 门户那条线） |

---

## 第三步：签抖音（780 MB + 8 扩展）← 验证内存与 CPU 预算

- [ ] 能**签完**（换之前会在这阶段闪退）
- [ ] 装完能打开
- [ ] 🔴 **签名期间，后台其他 App 会不会被杀**（换之前：网易云 + LocalDevVPN 一起被杀）

**对比基线**（换之前）：内存峰值 **2.11 GB**（`JetsamEvent`：`rpages 129697 × 16KB`）。
换之后应该明显更低 —— 上游是「mmap 读 + 复制后改」，全程只有 1 份内存 ✓。

---

## 第四步：出问题就把日志给我（**日志是唯一的排障通道**）

导出：**设置 → 隐私与安全性 → 分析与改进 → 分析数据 → 找 `Seal-2026-09-20-…`**

我拿到日志会先做两件事：

1. **定版** —— `grep 构建`，必须是 `构建 1.1.16 (175)`。
   ⚠️ **没有这一行说明你装的还是旧包**（早于 2026-09-17 的包没有这行）⇒ 重新装再测。
2. **分清「崩溃」还是「被杀」**：
   - 日志**戛然而止** + 有 `Seal.cpu_resource-*.ips`（`bug_type 202`、正文 `Event: cpu usage`）
     ⇒ 撞了 **iOS 的 CPU 预算**（任意 180 秒内不得超过 90 秒 CPU）**不是崩溃** ✗
   - 有异常栈的崩溃日志 ⇒ 才是代码崩了

---

## 这一轮要回答的三个问题（换签名器的代价）

| # | 问题 | 严重级 | 看哪一步 |
|---|---|---|---|
| 1 | **装完能不能启动**（上游不处理 FairPlay `cryptid` 清零） | 🔴 最高 | 第二步 |
| 2 | **大包会不会被 CPU 预算杀掉**（上游没有签名缓存 ⇒ 全量重签） | 🔴 高 | 第三步 |
| 3 | **日志里还能不能看出「死在哪个 bundle」**（逐 bundle 诊断没了） | 🟡 中 | 第四步 |

> 第 3 条是**已知的能力退化** ✓（不是 bug）：上游的逐 bundle 日志走 `print`，
> **进不了 Seal 的导出日志** ✗。真机若再出现「签名阶段日志空白」，
> 可以考虑接上游 `signApp(progress:)`（**上游自己的 API** ✓，不算发明 ✓）。
